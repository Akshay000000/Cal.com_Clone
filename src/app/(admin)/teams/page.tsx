import UpgradeView from "@/components/admin/UpgradeView";

export default function TeamsPage() {
  return (
    <div>
      <UpgradeView 
        featureTitle="Collaborate and schedule with your team" 
        featureDesc="Create round-robin events, collective meetings, and manage availability across your organization." 
      />
    </div>
  );
}
