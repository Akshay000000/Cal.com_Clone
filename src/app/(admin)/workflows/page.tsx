import UpgradeView from "@/components/admin/UpgradeView";

export default function WorkflowsPage() {
  return (
    <div>
      <UpgradeView 
        featureTitle="Automate your scheduling communications" 
        featureDesc="Send automated emails, SMS reminders, and trigger webhooks based on booking events." 
      />
    </div>
  );
}
